# Databricks App 설정
# 배포 후 UI에서 SQL Warehouse 리소스를 연결해야 합니다.
command: ["streamlit", "run", "app.py", "--server.port", "8000"]

env:
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: "sql-warehouse"        # UI에서 리소스 연결 시 자동 매핑
